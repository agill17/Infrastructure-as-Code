### ci job will run -- build, 
### unit tests, lints,
### deploy artifact to s3 or nexus
### trigger selenium ? -- maybe
### run deploy job 
#      -- run tf and make sure infra is ready
#      -- then run recipes or playbooks on servers
#         -- config-mng: setup app servers and deploy artifacts to app servers

# run all this as part of pipeline job. yea lets try that out